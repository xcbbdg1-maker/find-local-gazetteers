---
name: find-local-gazetteers
description: 查找中国特定地区的县志、府志、州志、市志、省志、乡镇志及其续志，核对行政区沿革、书名版本和在线获取方式。用户说“帮我找县志”“哪里能看某地地方志”“找某县历代县志”“有没有在线阅读或 PDF”“查某地旧志/市志/府志”时使用；适合偶尔查找个别志书，不用于批量抓取全国县志或搭建大型方志数据库。
---

# 县志查找

目标是找到少量真正可用的志书，并准确说明其版本、来源和获取状态。不要把书目页误报成全文，不要为了数量堆积低可信链接。

## 工作流

### 1. 确定地域范围

提取现代地名、用户关心的年代和期望获取方式。地域可能有歧义时不要立刻阻塞：同时搜索现代行政区与最可能的历史辖区，并在结果中说明差异；只有不同解释会产生完全不同任务时才询问。

建立最小沿革表：

- 当前行政区及上级辖区；
- 曾用县、市、州、府、厅名称；
- 撤县设市、并县、分县或改区关系；
- 可能覆盖该地的上级志书。

### 2. 扩展书名与检索词

至少组合以下形式：

- `地名 + 县志 / 市志 / 府志 / 州志 / 厅志`
- `地名 + 旧志 / 续志 / 志略 / 乡土志`
- `书名 + 在线阅读 / 全文 / PDF / 馆藏 / 目录`
- `书名 + 编纂委员会 / 出版社 / 年份`
- 历史地名与现代地名分别搜索

若目标属于上海、靖江、大连或海南，读取 [references/seed-targets.md](references/seed-targets.md) 后再搜索。

### 3. 按来源优先级搜索

使用可用的联网搜索工具；若 `agent-reach` 存在，遵循其搜索与网页阅读路由。

官方数字方志站若使用动态页面，文本阅读器可能返回空内容。此时改用可见浏览器打开并操作站内检索；不能因为搜索引擎未收录或文本抓取为空就判定“没有该志书”。

若官网在浏览器中超时、断开连接或临时维护，记录当次访问日期和失败现象，将其标为`链接失效或待核实`，再转向国家图书馆、地方公共图书馆及政府转载页核对书目。站点故障不等于志书不存在，也不能沿用旧的“全文在线”状态。

依次检查：

1. 地方志办公室、省情网、市情网、政府数字方志馆；
2. 国家及地方公共图书馆、大学图书馆、档案馆；
3. 国家级或机构建设的古籍、方志数据库；
4. Google Books、CiNii 等书目或馆藏联合目录；
5. 专业县志网站、个人整理项目，仅作为补充线索。

详细评级、检索模板和获取状态见 [references/source-priority.md](references/source-priority.md)。

### 4. 逐条验证

打开候选页面，不只阅读搜索摘要。每条至少核对：

- 题名及地域是否匹配；
- 修纂者、成书或出版年代、版本；
- 记述上下限；
- 页面主办方；
- 是书目、目录、局部预览、全文阅读还是直接下载；
- 是否需要登录、读者证、机构网络或付费；
- 链接当前是否可访问。

无法核实的字段写“未核实”，不得补猜。只有实际打开阅读器或文件链接后，才能标记“可在线阅读”或“可下载”。

### 5. 形成短名单

每个地区默认保留 3–8 个最有用结果，优先覆盖：

- 一部现代综合志；
- 一至数部重要旧志；
- 一个可靠的在线获取入口；
- 找不到全文时的馆藏或申请路径。

同一版本的镜像链接只保留可信度最高的一条，其余作为备用。

## 输出格式

先说明地名沿革或范围判断，再使用表格：

| 志书 | 年代/版本 | 覆盖范围 | 获取状态 | 来源与可信度 | 建议 |
|---|---|---|---|---|---|

获取状态只能使用：

- `全文在线`
- `可下载`
- `局部预览`
- `仅目录/书目`
- `馆藏可查`
- `需登录/机构权限`
- `链接失效或待核实`

结尾给出“先看哪一本”的简短建议，并列出仍缺失的版本。用户只想找书时，不扩展成历史分析。

## 边界

- 不绕过登录、验证码、付费墙或下载限制。
- 不把来源不明的网盘文件作为首选；如必须列出，标为低可信线索。
- 不宣称古代志书一定无版权；扫描件、点校本、数据库和现代志书可能另有权利。
- 不批量下载。只有用户明确要求下载且来源允许时才执行，并记录来源与文件名。
- 找不到全文也要交付可核验的书目信息和下一步获取路径。
